# Core Framework
Flask==3.0.0
Werkzeug==3.0.1

# Flask Dependencies (explicit)
Jinja2>=3.1.2
MarkupSafe>=2.1.3
itsdangerous>=2.1.2
click>=8.1.7
blinker>=1.6.2

# WSGI Production Servers
waitress>=2.1.2
gunicorn>=21.2.0; sys_platform != 'win32'

# Database (SQLite is built-in, but include backup utilities)
# Note: sqlite3 is included with Python

# Additional utilities
python-dateutil>=2.8.2
